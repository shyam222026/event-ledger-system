Assistant instructions for working on this repository.

- Be concise, make minimal changes, and run `mvn clean verify` after edits.
- Use `jacoco-maven-plugin` for coverage and ensure reports are generated under `target/site/jacoco`.
- Add or modify files only within the repository; do not modify external environment.
- When making edits, update this file and `prompt.md` to document intended behavior.

When finished, provide a short summary: what changed, build output status, and where to find coverage reports.
